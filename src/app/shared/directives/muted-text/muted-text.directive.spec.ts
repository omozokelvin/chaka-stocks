import { MutedTextDirective } from './muted-text.directive';

describe('MutedTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MutedTextDirective();
    expect(directive).toBeTruthy();
  });
});
