import { SpacedHeaderDirective } from './spaced-header.directive';

describe('SpacedHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SpacedHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
