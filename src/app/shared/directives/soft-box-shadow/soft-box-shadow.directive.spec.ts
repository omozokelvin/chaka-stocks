import { SoftBoxShadowDirective } from './soft-box-shadow.directive';

describe('SoftBoxShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new SoftBoxShadowDirective();
    expect(directive).toBeTruthy();
  });
});
